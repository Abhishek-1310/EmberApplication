{{page-title "Details"}}
<h1>{{@model.title}}</h1>
{{!-- <h1>hello</h1> --}}
{{!-- <button class="btn1" {{action "deleteRecipe" model.id}} >Delete</button> --}}
<h3 class="text-center">{{@model.description}}</h3>
<div class="recipe-info-section">
    <h3>Ingredients</h3>
    <ol>
        {{#each @model.ingredients as |ingredient|}}
            <li>{{ingredient}}</li>
        {{/each}}
    </ol>

</div>
<div class="recipe-info-section">
    <h3>instructions</h3>
    <p>{{@model.instructions}}</p>

</div>